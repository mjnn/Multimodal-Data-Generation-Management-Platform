# 手工验收清单 — M1.4

| 字段 | 内容 |
|------|------|
| 工单 | M1.4 · 路由门禁 + 角色菜单 + OSS 权限 |
| 日期 | 2026-07-21 |
| 前置 | 后端 8001 + 前端 5174 已启动；多角色测试账号 |

## 角色菜单（登录后看侧边栏）

| 账号 | 密码 | 应见菜单 | 不应见 |
|------|------|----------|--------|
| `admin` | `admin123` | 总览、检索、OSS、**用户管理** | — |
| `m12_test_reviewer_7e5a2c1a` | `reviewpass123` | 总览、检索 | OSS、用户管理 |
| （若有 dataset_manager 账号） | — | 总览、检索、OSS | 用户管理 |

## 路由门禁

1. [ ] reviewer 直接访问 `/oss` → **403 页面**
2. [ ] reviewer 直接访问 `/admin/users` → **403 页面**
3. [ ] admin 访问 `/admin/users` → 用户列表，可新建/编辑
4. [ ] admin 在用户管理创建 reviewer → 成功

## 后端 ACL（可选 curl）

```powershell
# reviewer token 访问 OSS → 403
py -3 hmi/backend/scripts/test_auth_m14.py
```

## 自动化

- `npm run build` — exit 0
- `py -3 hmi/backend/scripts/test_auth_m14.py` — 全 OK

## 点测结论（用户填写）

- [ ] 全部通过
- [ ] 部分失败（注明编号）：
