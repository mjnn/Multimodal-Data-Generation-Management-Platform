# M3.2 · AI 聚合 + enqueue · 验收清单

> 格式：`project-management/acceptance/_FORMAT.md`

| 字段 | 内容 |
|------|------|
| 工单 | M3.2 · AI 聚合 + enqueue |
| 日期 | 2026-07-21 |
| Agent 自动化摘要 | A-1～A-5 通过；无 A-E2E |

---

## 一、Agent / 自动化

### A · 单元 / API / 构建

#### A-1 · R4 frame_first 聚合

**操作步骤**
1. 多帧 `fact_image_label` → `aggregate_clip_labels`
2. 按 `frame_id` 去重取首条

**通过判断标准**
- `aggregation == frame_first`
- `labels_json` 为 clip 级扁平 dict

**执行记录**
- 2026-07-21：通过

---

#### A-2 · R4 sync_group 聚合

**操作步骤**
1. 同 sync_group 多帧，含 `label_scope=sync_group` 行
2. 优先取 sync_group 行 labels

**执行记录**
- 2026-07-21：通过

---

#### A-3 · enqueue_clip 创建

**操作步骤**
1. Job3 完成 + 有标签 → `enqueue_clip` → `pending_review`

**通过判断标准**
- `taxonomy_version_id` 来自 published（可为 null）
- `ai_source_summary_json` 含 aggregation 元数据

**执行记录**
- 2026-07-21：通过

---

#### A-4 · 重复入队跳过

**操作步骤**
1. 同一 `(clip_id, run_id)` 再次 enqueue → skipped

**执行记录**
- 2026-07-21：通过

---

#### A-5 · CLI + 候选扫描

**操作步骤**
1. `hmi/scripts/enqueue_review_clips.py --list-candidates`
2. `list_enqueue_candidates()` 排除已有 review 行

**证据**
- `py -3 hmi/backend/scripts/test_review_m32.py` 全绿

**执行记录**
- 2026-07-21：通过

---

### A-E2E

本工单无 A-E2E。

---

## 二、H

无。

---

## 三、点测结论

- [x] A 全绿 → **M3.2 done**
