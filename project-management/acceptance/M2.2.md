# M2.2 · YAML 导入 + bootstrap 脚本 · 验收清单

> 格式：`project-management/acceptance/_FORMAT.md`

| 字段 | 内容 |
|------|------|
| 工单 | M2.2 · YAML 导入 + bootstrap 脚本 |
| 日期 | 2026-07-21 |
| 环境前置 | 本地 app.db；`config/oms_label_taxonomy.yaml` 存在 |
| Agent 自动化摘要 | A-1～A-4 通过；本工单无 A-E2E |

---

## 一、Agent / 自动化

### A · 单元 / API / 构建

#### A-1 · 解析 YAML 为 68 条 node

**操作步骤**
1. `py -3 hmi/backend/scripts/test_taxonomy_m22.py`（内含 `parse_taxonomy_yaml` / `yaml_labels_to_nodes`）

**期望结果**
- `label_count == 68`；首条 `label_id == L1.1.timestamp`

**通过判断标准**
- 脚本 exit 0，断言全部通过

**执行记录**
- 2026-07-21：通过

---

#### A-2 · 首次导入创建 draft

**操作步骤**
1. 同上测试脚本（`version_code=m22_test_import`）

**期望结果**
- `action` 为 `created` 或 `updated`；`count_nodes == 68`

**通过判断标准**
- 测试输出 `OK first import -> created nodes=68`

**执行记录**
- 2026-07-21：通过

---

#### A-3 · 幂等 skip

**操作步骤**
1. 连续两次导入同一 `version_code`（第二次无 `--force`）

**期望结果**
- 第二次 `action == skipped`，节点数仍为 68

**通过判断标准**
- 测试输出 `OK idempotent skip`

**执行记录**
- 2026-07-21：通过

---

#### A-4 · CLI bootstrap 默认版

**操作步骤**
1. `py -3 hmi/scripts/import_taxonomy_yaml.py`
2. 再次执行同命令

**期望结果**
- 首次 `created`（`version_code` 取自 YAML `version`，当前为 `v2`），68 节点
- 再次 `skipped`

**通过判断标准**
- CLI exit 0；第二次打印 `skipped`

**执行记录**
- 2026-07-21：通过（首次 `created v2` 68 nodes；二次 `skipped`）

---

### A-E2E · Playwright / Selenium

本工单无 A-E2E（无前端改动）。

---

## 二、人工签字 / 主观（H · 可选）

无。

---

## 三、不在本工单范围

- REST API（M2.3）
- 前端 Taxonomy 页（M2.5）
- `--publish` 端到端与 OSS（M2.6；`publish_version` 已实现供 CLI 使用）

---

## 四、点测结论

- [x] A 全绿
- [x] 无 A-E2E
- [x] 无 H
- [x] 可标 **M2.2 done**
