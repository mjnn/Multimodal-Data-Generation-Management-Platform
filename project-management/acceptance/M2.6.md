# M2.6 · OSS 导出 + dispatch + MC DDL · 验收清单

> 格式：`project-management/acceptance/_FORMAT.md`

| 字段 | 内容 |
|------|------|
| 工单 | M2.6 · OSS 导出 + dispatch + MC DDL |
| 日期 | 2026-07-21 |
| 环境前置 | app.db；OSS settings（export 用 mock 验证） |
| Agent 自动化摘要 | A-1～A-4 通过；无 A-E2E |

---

## 一、Agent / 自动化

### A · 单元 / API / 构建

#### A-1 · YAML 序列化

**操作步骤**
1. `nodes_to_yaml_document` 单元断言（`test_taxonomy_m26.py`）

**期望结果** · **通过判断标准**
- `label_count` 与 nodes 一致；含 `id`/`level_code`

**执行记录**
- 2026-07-21：通过

---

#### A-2 · dispatch merge

**操作步骤**
1. `merge_taxonomy_into_dispatch` / `attach_taxonomy_to_dispatch_payload`

**期望结果** · **通过判断标准**
- payload 含 `taxonomy_version_id` / `taxonomy_oss_key`

**执行记录**
- 2026-07-21：通过

---

#### A-3 · publish 挂钩 export（mock OSS）

**操作步骤**
1. `POST /api/taxonomy/versions/{id}/publish`（mock put/get OSS）

**期望结果** · **通过判断标准**
- 写入 `config/taxonomy/{code}.yaml`、`config/taxonomy/latest.json`
- 合并 `pipeline/dispatch/latest.json`
- 更新 `data/app_meta.json`

**执行记录**
- 2026-07-21：通过

---

#### A-4 · MC DDL 脚本

**操作步骤**
1. 检查 `sql/maxcompute/migrate_fact_image_label_taxonomy.sql`

**期望结果** · **通过判断标准**
- 含 `taxonomy_version_id STRING` ADD COLUMNS

**执行记录**
- 2026-07-21：通过

**证据**
- `py -3 hmi/backend/scripts/test_taxonomy_m26.py` 全绿

---

### A-E2E · Playwright / Selenium

本工单无 A-E2E（无前端改动）。

---

## 二、人工签字 / 主观（H · 可选）

无。

---

## 三、不在本工单范围

- Job3 写 MC `taxonomy_version_id` 列（stub/下一迭代）
- M2 全量验收（M2.7）

---

## 四、点测结论

- [x] A 全绿
- [x] 无 A-E2E / H
- [x] 可标 **M2.6 done**
