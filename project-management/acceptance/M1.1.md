# 手工验收清单 — M1.1

| 字段 | 内容 |
|------|------|
| 工单 | M1.1 · 应用 DB + JWT 认证后端 |
| 日期 | 2026-07-21 |
| 前置 | `HMI_JWT_SECRET` 已配置；`py -3 hmi/backend/scripts/test_auth_m11.py` 或启动 backend |

## 清单

1. [ ] `hmi/data/app.db` 存在且含 `app_user` / `app_user_role` 表
2. [ ] `GET /api/health` 无 token → **200**
3. [ ] `GET /api/clips` 无 token → **401**，body 含 `401_UNAUTHORIZED`
4. [ ] `POST /api/auth/login` 错误密码 → **401**
5. [ ] `POST /api/auth/login` 正确用户 → **200**，返回 `access_token` + Set-Cookie
6. [ ] `GET /api/auth/me` 带 Bearer token → **200**，roles 正确
7. [ ] `GET /api/clips` 带 Bearer token → **200**
8. [ ] `POST /api/auth/logout` → **200**，cookie 清除

## 自动化

```powershell
cd hmi/backend
$env:HMI_JWT_SECRET="your-secret-at-least-32-chars-long!!"
$env:HMI_DATA_SOURCE="local"
py -3 scripts/test_auth_m11.py
```

预期：全部 `OK ...` 行，exit 0。

## 不在本工单范围

- `bootstrap_admin.py`、管理员 CRUD API（M1.2）
- 前端登录页（M1.3）
- 角色菜单与 OSS 写权限（M1.4）

## 点测结论（用户填写）

- [ ] 全部通过
- [ ] 部分失败（注明编号）：
