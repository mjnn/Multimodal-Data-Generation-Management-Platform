# 手工验收清单 — M1.3

| 字段 | 内容 |
|------|------|
| 工单 | M1.3 · 前端登录与 Auth 上下文 |
| 日期 | 2026-07-21 |
| 前置 | 后端已启动；已有 admin 账号（`bootstrap_admin.py`） |

## 清单

1. [ ] 未登录直接访问 `http://localhost:5173/` → 重定向 `/login`
2. [ ] 登录页输入错误密码 → 显示错误提示
3. [ ] 正确账号登录 → 进入 Clip 总览，数据可加载
4. [ ] 刷新页面 → 仍保持登录（refresh cookie）
5. [ ] 点击右上角「退出」→ 回到 `/login`
6. [ ] 退出后再访问 `/` → 需重新登录
7. [ ] 登录前访问 `/search`，登录后 → 进入 `/search`（from 跳转）

## 启动命令

```powershell
# 终端 1 — 后端
cd backend
$env:HMI_JWT_SECRET="your-secret-at-least-32-characters-long"
py -3 run.py

# 终端 2 — 前端
cd frontend
npm run dev
```

## 自动化

```powershell
cd frontend
npm run build
```

预期：exit 0，无 TS 错误。

## 不在本工单范围

- 按角色隐藏 OSS / 用户管理菜单（M1.4）
- AdminUsersPage（M1.4）

## 点测结论（用户填写）

- [ ] 全部通过
- [ ] 部分失败（注明编号）：
