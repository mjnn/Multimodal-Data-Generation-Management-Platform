# 手工验收清单 — M1.2

| 字段 | 内容 |
|------|------|
| 工单 | M1.2 · 管理员用户 API + bootstrap |
| 日期 | 2026-07-21 |
| 前置 | M1.1 已完成；`HMI_JWT_SECRET` 已配置 |

## 清单

1. [ ] `py -3 hmi/scripts/bootstrap_admin.py --username admin --password <8+chars> --force-new` 创建 admin（或已存在时提示 skip）
2. [ ] admin 登录后 `GET /api/admin/users` → **200**，返回用户列表
3. [ ] admin `POST /api/admin/users` 创建 reviewer → **201**
4. [ ] admin `PATCH /api/admin/users/{id}` 改 display_name → **200**
5. [ ] reviewer 登录后 `POST /api/admin/users` → **403**（`403_FORBIDDEN`）
6. [ ] 短密码（<8）创建用户 → **422**

## 自动化

```powershell
cd hmi/backend
$env:HMI_JWT_SECRET="your-secret-at-least-32-characters-long"
py -3 scripts/test_auth_m12.py
```

预期：全部 `OK ...` 行，exit 0。

## 不在本工单范围

- 前端 AdminUsersPage（M1.4）
- 前端登录页（M1.3）

## 点测结论（用户填写）

- [ ] 全部通过
- [ ] 部分失败（注明编号）：
