# 手工验收清单 — M2.1

| 字段 | 内容 |
|------|------|
| 工单 | M2.1 · Taxonomy DB + taxonomy_db.py |
| 日期 | 2026-07-21 |

## 清单

1. [ ] `hmi/data/app.db` 含 `label_taxonomy_version`、`label_taxonomy_node` 表
2. [ ] 可 `create_version`（draft）
3. [ ] `replace_nodes` 写入节点；`list_nodes` / `count_nodes` 正确
4. [ ] 同一 version 内 `label_id` 唯一
5. [ ] 非 draft 版本 `replace_nodes` → ValueError

## 自动化

```powershell
cd backend
py -3 scripts/test_taxonomy_m21.py
```

## 点测结论（用户填写）

- [ ] 全部通过
