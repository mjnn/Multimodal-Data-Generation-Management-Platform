---
description: HMI 本地数据源（SQLite + 磁盘 OSS 模拟）与在线 OSS/MC
globs: hmi/backend/**,hmi/frontend/**,hmi/scripts/**
alwaysApply: false
---

# HMI 数据源

## 模式

| `data_source` | 元数据 | 文件 |
|---------------|--------|------|
| **local** | `hmi_runtime/hmi.db` | **`oss/`**（模拟云端桶）+ **`artifacts/`**（同步后 runtime） |
| **cloud** | MaxCompute | 阿里云 OSS |

UI 侧栏 **本地模式 / 在线模式** → `GET/POST /api/config/data-source`。切换后刷新页面；后端 `router.py` 在 local 时用 `clips_local` / `search_local`。

## 本地运行时

- 默认根：`hmi/data/hmi_runtime/`（`HMI_RUNTIME_ROOT` / `shared/repo_paths.HMI_RUNTIME_ROOT`）
- 初始化：`hmi/scripts/init_local_runtime.py`
- ECS：将 `HMI_RUNTIME_ROOT` 指向挂载盘即可视为本地模式

## 本地 OSS 模拟（sdk_v1）

- **云端等价物**：`hmi_runtime/oss/`（rosbags、clips、pipeline/dispatch、config/taxonomy、…）
- **Clip 产物路径**：`oss/clips/{clip_id 中 `:` → `__}/runs/{run_id}/`（jsonl + preview/ + run.json）
- **SDK worker**（`local_sdk_worker`）：infer → import 生成 preview → **mirror 到 `oss/clips/`** → 写 `pipeline/dispatch/latest.json`
- **禁止**依赖 `sync_sandbox_pipeline.py`（已废弃）；新 bag 走上传 + worker

## OSS 管理（local）

- `GET /api/oss/*` 在 `data_source=local` 时读写 **`LOCAL_OSS_ROOT`**，勿连真实 bucket
- **`bucket` 展示名**：`本地磁盘`；`endpoint` 为 oss 根目录绝对路径；`simulated: true`
- 前端 `OssManagePage` 随 data-source 切换重新拉 info/list

## OSS → runtime 同步

- OSS 管理 **「自动同步 OSS 产物到本地」** → `oss_sync_poller`；local 下读 **本地** dispatch，执行 `sync_runtime_from_local_oss`（oss/clips → artifacts + SQLite ingest）
- 线上仍用 `hmi/scripts/sync_hmi_local.py`（MC+OSS）
- 启动可选：`HMI_MIRROR_ARTIFACTS_TO_OSS=1` 将已有 artifacts 回填到 oss/clips

## 本地 SDK 轮询

- `HMI_LOCAL_SDK_POLL_ENABLED`（local 默认开）扫描 `local://rosbags/` 登记项
- 步骤写 `pipeline_step`；总览读 `hmi.db`

## HMI 产物 baseline

侧栏 **重置 HMI 产物**（admin）→ `POST /api/hmi/reset-artifacts`：校核/派单/数据集/用户(除 admin)/标签树 → 仅 `label_tree_baseline`；**不删** clip 管线产物。

## 演示 Clip

`sha256:demo_*` 通过 `seed_demo_clip_data.py` 写入本地 clip SQLite。

## 来源

沉淀自 session 2026-07-27（本地 OSS 模拟 + SDK worker + 同步 + OSS UI）
